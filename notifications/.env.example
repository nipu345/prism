PORT=4000
NOTIFY_API_KEY=change-me

# Leave SMTP_HOST unset to use an auto-generated Ethereal test inbox —
# preview links are logged to the console, no real credentials needed.
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
FROM_EMAIL="Prism <notifications@yourdomain.com>"
